"""This will be our service to orchestrate our Airflow pipeline

Step 1: Build API ingestion system -> single calls to keep it simple, will later move to a batch approach
Step 2: Create the DAG with S3 connector
"""